const data = {
    "title": "SQL Command Cheatsheet",
    "description": "A comprehensive guide to the SQL language, covering data querying, manipulation, definition, joins, functions, and advanced concepts.",
    "categories": [
        {
            "title": "Data Query and Manipulation (DML/DQL)",
            "description": "Commands for querying, adding, deleting, and changing data within tables.",
            "items": [
                {
                    "title": "SELECT - Querying Data",
                    "description": "The fundamental command for retrieving data from database tables.",
                    "table": {
                        "headers": ["Clause", "Description"],
                        "rows": [
                            ["SELECT col1, col2", "Specifies the columns to return."],
                            ["FROM table_name", "Specifies the table to retrieve data from."],
                            ["WHERE condition", "Filters records based on a specific condition."],
                            ["ORDER BY col", "Sorts the result set in ascending (ASC) or descending (DESC) order."],
                            ["LIMIT N / TOP N", "Restricts the number of rows returned (syntax varies by SQL dialect)."],
                            ["DISTINCT col", "Returns only unique values in the specified column."]
                        ]
                    },
                    "example": "SELECT first_name, last_name, email\nFROM users\nWHERE registration_date > '2023-01-01'\nORDER BY last_name ASC\nLIMIT 100;"
                },
                {
                    "title": "INSERT - Adding Data",
                    "description": "Adds one or more new rows to a table.",
                    "table": null,
                    "example": "INSERT INTO employees (id, first_name, last_name, department_id)\nVALUES (101, 'John', 'Smith', 5);"
                },
                {
                    "title": "UPDATE - Modifying Data",
                    "description": "Modifies existing records in a table. Always use a WHERE clause!",
                    "table": null,
                    "example": "UPDATE products\nSET price = 49.99, on_sale = TRUE\nWHERE product_id = 789;"
                },
                {
                    "title": "DELETE - Removing Data",
                    "description": "Deletes existing records from a table. Always use a WHERE clause!",
                    "table": null,
                    "example": "DELETE FROM logs\nWHERE log_date < '2022-01-01';"
                }
            ]
        },
        {
            "title": "Joining Multiple Tables",
            "description": "Commands for combining rows from two or more tables based on a related column between them.",
            "items": [
                {
                    "title": "Types of Joins",
                    "description": null,
                    "table": {
                        "headers": ["Join Type", "Description"],
                        "rows": [
                            ["INNER JOIN", "Returns records that have matching values in both tables."],
                            ["LEFT JOIN", "Returns all records from the left table, and the matched records from the right table."],
                            ["RIGHT JOIN", "Returns all records from the right table, and the matched records from the left table."],
                            ["FULL OUTER JOIN", "Returns all records when there is a match in either the left or right table."],
                            ["CROSS JOIN", "Returns the Cartesian product of the sets of records from the two joined tables."]
                        ]
                    },
                    "example": "SELECT o.order_id, c.customer_name, o.order_date\nFROM orders AS o\nINNER JOIN customers AS c ON o.customer_id = c.id;"
                }
            ]
        },
        {
            "title": "Aggregate Functions",
            "description": "Functions that perform a calculation on a set of rows and return a single summary value. Often used with GROUP BY.",
            "items": [
                {
                    "title": "Common Functions",
                    "description": null,
                    "table": {
                        "headers": ["Function", "Description"],
                        "rows": [
                            ["COUNT(column)", "Counts the number of non-NULL rows."],
                            ["SUM(column)", "Calculates the sum of a numeric column."],
                            ["AVG(column)", "Calculates the average value of a numeric column."],
                            ["MIN(column)", "Returns the minimum value in a column."],
                            ["MAX(column)", "Returns the maximum value in a column."]
                        ]
                    },
                    "example": "SELECT department_id, COUNT(id) AS number_of_employees, AVG(salary) AS average_salary\nFROM employees\nGROUP BY department_id\nHAVING COUNT(id) > 10;"
                }
            ]
        },
        {
            "title": "Data Definition Language (DDL)",
            "description": "Commands for defining and managing database objects like tables, columns, and indexes.",
            "items": [
                {
                    "title": "Table Management",
                    "description": null,
                    "table": {
                        "headers": ["Command", "Description"],
                        "rows": [
                            ["CREATE TABLE", "Creates a new table in the database."],
                            ["ALTER TABLE", "Modifies an existing table (adds, deletes, or modifies columns)."],
                            ["DROP TABLE", "Deletes a table and all its data."],
                            ["TRUNCATE TABLE", "Deletes all data from a table quickly, but keeps the table structure."]
                        ]
                    },
                    "example": "CREATE TABLE employees (\n    id INT PRIMARY KEY,\n    first_name VARCHAR(50) NOT NULL,\n    last_name VARCHAR(50) NOT NULL,\n    hire_date DATE,\n    salary DECIMAL(10, 2)\n);\n\nALTER TABLE employees\nADD COLUMN email VARCHAR(100) UNIQUE;"
                }
            ]
        },
        {
            "title": "Common Functions (String, Numeric, Date)",
            "description": "Built-in functions for manipulating data. Syntax may vary slightly between SQL dialects (PostgreSQL, SQL Server, etc.).",
            "items": [
                {
                    "title": "String Functions",
                    "description": null,
                    "table": {
                        "headers": ["Function", "Description"],
                        "rows": [
                            ["CONCAT(str1, str2)", "Joins two or more strings together."],
                            ["LOWER(str) / UPPER(str)", "Converts a string to lowercase or uppercase."],
                            ["TRIM(str)", "Removes leading and trailing whitespace."],
                            ["SUBSTRING(str, start, length)", "Extracts a substring of a certain length."],
                            ["LENGTH(str)", "Returns the number of characters in a string."]
                        ]
                    },
                    "example": null
                },
                {
                    "title": "Date and Time Functions",
                    "description": null,
                    "table": {
                        "headers": ["Function", "Description"],
                        "rows": [
                            ["NOW() / CURRENT_TIMESTAMP", "Returns the current date and time."],
                            ["EXTRACT(part FROM date)", "Extracts a part (YEAR, MONTH, DAY) from a date."],
                            ["DATE_ADD(date, INTERVAL val unit)", "Adds a time/date interval to a date value (syntax varies)."]
                        ]
                    },
                    "example": "SELECT order_id, EXTRACT(YEAR FROM order_date) AS order_year FROM orders;"
                }
            ]
        },
        {
            "title": "Conditional Expressions",
            "description": "Logic for handling if/then/else scenarios directly within a query.",
            "items": [
                {
                    "title": "CASE Statement",
                    "description": "Goes through conditions and returns a value when the first condition is met.",
                    "table": null,
                    "example": "SELECT order_id, order_total,\n       CASE\n           WHEN order_total > 500 THEN 'High Value'\n           WHEN order_total > 100 THEN 'Medium Value'\n           ELSE 'Low Value'\n       END AS order_category\nFROM orders;"
                },
                {
                    "title": "COALESCE and NULLIF",
                    "description": "Shortcuts for handling NULL values.",
                    "table": {
                        "headers": ["Function", "Description"],
                        "rows": [
                            ["COALESCE(val1, val2, ...)", "Returns the first non-NULL value in a list."],
                            ["NULLIF(expr1, expr2)", "Returns NULL if the two expressions are equal; otherwise, returns the first expression."]
                        ]
                    },
                    "example": "SELECT product_name, COALESCE(sale_price, list_price) AS final_price FROM products;"
                }
            ]
        },
        {
            "title": "Advanced Querying Concepts",
            "description": "Powerful constructs for writing complex and efficient queries.",
            "items": [
                {
                    "title": "Subqueries (Nested Queries)",
                    "description": "A query nested inside another query, often within a WHERE, FROM, or SELECT clause.",
                    "table": null,
                    "example": "SELECT employee_name\nFROM employees\nWHERE department_id IN (SELECT id FROM departments WHERE location = 'New York');"
                },
                {
                    "title": "Common Table Expressions (CTEs)",
                    "description": "A temporary, named result set created with a `WITH` clause that you can reference within a SELECT, INSERT, UPDATE, or DELETE statement.",
                    "table": null,
                    "example": "WITH regional_sales AS (\n    SELECT region, SUM(amount) AS total_sales\n    FROM sales\n    GROUP BY region\n)\nSELECT region, total_sales\nFROM regional_sales\nWHERE total_sales > 1000000;"
                },
                {
                    "title": "Window Functions",
                    "description": "Performs a calculation across a set of table rows that are related to the current row.",
                    "table": null,
                    "example": "SELECT employee_name, salary,\n       RANK() OVER (PARTITION BY department_id ORDER BY salary DESC) as rank_in_department\nFROM employees;"
                }
            ]
        },
        {
            "title": "Transaction and Data Control (TCL/DCL)",
            "description": "Commands for managing transactions to ensure data integrity and for controlling user access permissions.",
            "items": [
                {
                    "title": "Transaction Control",
                    "description": null,
                    "table": {
                        "headers": ["Command", "Description"],
                        "rows": [
                            ["START TRANSACTION", "Begins a new transaction block."],
                            ["COMMIT", "Saves all changes made in the transaction permanently."],
                            ["ROLLBACK", "Undoes all changes made in the current transaction."]
                        ]
                    },
                    "example": "START TRANSACTION;\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;\nCOMMIT;"
                },
                {
                    "title": "Data Control",
                    "description": null,
                    "table": {
                        "headers": ["Command", "Description"],
                        "rows": [
                            ["GRANT", "Gives specific permissions (e.g., SELECT, UPDATE) on a database object to a user."],
                            ["REVOKE", "Removes previously granted permissions from a user."]
                        ]
                    },
                    "example": "GRANT SELECT, INSERT ON customers TO 'readonly_user'@'localhost';"
                }
            ]
        }
    ]
}
